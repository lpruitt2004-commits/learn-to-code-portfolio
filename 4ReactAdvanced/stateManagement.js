
// Practice exercises for state management (Redux, Zustand, etc.)
// Welcome! This file will help you practice advanced state management in React.

// Example: Redux basics
// import { createStore } from 'redux';
// function counterReducer(state = { count: 0 }, action) {
//   switch (action.type) {
//     case 'increment': return { count: state.count + 1 };
//     case 'decrement': return { count: state.count - 1 };
//     default: return state;
//   }
// }
// const store = createStore(counterReducer);
// ai: What is Redux and when should I use it?

// TODO: Set up a Redux store for a simple counter app.

// Example: Zustand (lightweight state management)
// import create from 'zustand';
// const useStore = create(set => ({ count: 0, inc: () => set(state => ({ count: state.count + 1 })) }));
// ai: What are the benefits of Zustand over Redux?

// TODO: Use Zustand or another library to manage the state of a shopping cart.
